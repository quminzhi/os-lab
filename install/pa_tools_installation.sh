#!/usr/bin/env /usr/bin/bash
apt-get install build-essential -y   # build-essential packages, include binary utilities, gcc, make, and so on
apt-get install man             -y  # on-line reference manual
apt-get install gdb             -y   # GNU debugger
apt-get install git             -y   # revision control system
apt-get install libreadline-dev -y   # a library to use compile the project later
apt-get install libsdl2-dev     -y   # a library to use compile the project later
apt-get install libc6-dev-i386  -y   # a library to use compile the project later
apt-get install qemu-system     -y   # QEMU
apt-get install vim             -y   # VIM editor

# basic tools
apt-get install cscope          -y
apt-get install ctags-universal -y
